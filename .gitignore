# Secrets — never commit real environment files (or backups of them);
# the negation keeps the documented template tracked.
.env*
!.env.example

# Media libraries live on the VPS, not in git
music/*
!music/.gitkeep
visuals/*
!visuals/.gitkeep
# Starter loops ship with the repo so the first deploy looks branded — and
# rotates through a few scenes — before any Seedance clips exist.
!visuals/00-dusk-drift.mp4
!visuals/01-deep-night.mp4
!visuals/02-warm-sunset.mp4
downloads/*
!downloads/.gitkeep

# Runtime state (now-playing lives in the radio-data named volume, not a
# host dir; slskd keeps its app state in ./slskd)
slskd/
*.log

# OS noise
.DS_Store
